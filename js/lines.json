{
  "slop": [
    "const list = res.data.data.list || res.data.list || []",
    "if (user && user.profile && user.profile.role === 'admin') showAdmin()",
    "await fetch('/api/orders?page=' + page + '&size=' + size)",
    "const token = localStorage.getItem('token') || sessionStorage.token",
    "btn.onclick = function () { save(); close(); location.reload() }",
    "setTimeout(function () { pollStatus(id) }, 300)",
    "for (var i = 0; i < rows.length; i++) { rows[i].checked = true }",
    "try { JSON.parse(raw) } catch (e) { return {} }",
    "return { code: 0, msg: 'ok', data: payload }",
    "const name = form.name.value.trim() || 'untitled'",
    "el.style.display = visible ? 'block' : 'none'",
    "window.addEventListener('resize', function () { layout() })",
    "const price = Number(item.price) || 0",
    "if (status == 1 || status == '1') markPaid(order)",
    "document.getElementById('list').innerHTML = html",
    "axios.get('/user/info').then(function (r) { vm.user = r.data })",
    "const query = Object.assign({}, defaultQuery, filters)",
    "export default { name: 'OrderList', data: function () { return state } }",
    "router.push({ path: '/detail', query: { id: row.id } })",
    "const date = moment(row.createdAt).format('YYYY-MM-DD HH:mm')",
    "if (!this.form.phone) { this.$message.error('phone required'); return }",
    "this.loading = true; this.fetchList().finally(() => { this.loading = false })",
    "const headers = { Authorization: 'Bearer ' + getToken() }",
    "fs.readFileSync(path.join(__dirname, 'config.json'), 'utf8')",
    "app.use(cors()); app.use(express.json()); app.use('/api', routes)",
    "db.query('SELECT * FROM users WHERE id = ' + userId, callback)",
    "redis.get('session:' + sid, function (err, value) { next() })",
    "module.exports = { getUser, updateUser, deleteUser }",
    "const config = require('../config/' + process.env.NODE_ENV)",
    "if (err) { console.log(err); return res.status(500).send(err) }",
    "const total = cart.items.reduce(function (s, it) { return s + it.qty * it.price }, 0)",
    "table.onclick = function (e) { if (e.target.dataset.id) open(e.target.dataset.id) }",
    "const clone = JSON.parse(JSON.stringify(record))",
    "img.src = row.avatar || '/assets/default-avatar.png'",
    "history.replaceState(null, '', location.pathname + '?tab=' + tab)",
    "const qs = Object.keys(params).map(k => k + '=' + params[k]).join('&')",
    "if (type === 'edit') { this.mode = 'edit'; this.loadDetail(id) }",
    "clearTimeout(timer); timer = setTimeout(search, 400)",
    "const pageCount = Math.ceil(total / pageSize) || 1",
    "el.className = 'row' + (active ? ' is-active' : '')",
    "return fn.call(ctx, req, res, next)",
    "const idx = list.findIndex(function (item) { return item.id === id })",
    "if (idx > -1) list.splice(idx, 1)",
    "localStorage.setItem('filters', JSON.stringify(this.query))",
    "const file = req.files && req.files[0]",
    "res.setHeader('Content-Type', 'application/json; charset=utf-8')",
    "const env = process.env.API_BASE || 'http://127.0.0.1:8080'",
    "watch: { 'query.keyword': function () { this.page = 1; this.load() } }",
    "created() { this.load(); this.timer = setInterval(this.load, 15000) }",
    "beforeDestroy() { if (this.timer) clearInterval(this.timer) }"
  ],
  "good": [
    "const items = Array.isArray(payload.list) ? payload.list : []",
    "if (!user?.profile) return null",
    "const res = await fetch(`/api/orders?${new URLSearchParams(query)}`)",
    "const token = sessionStorage.getItem('token')",
    "button.addEventListener('click', onSave, { once: true })",
    "await new Promise((resolve) => setTimeout(resolve, 300))",
    "for (const row of rows) row.checked = true",
    "const data = JSON.parse(raw || '{}')",
    "return { ok: true, data }",
    "const name = String(form.name.value).trim() || 'untitled'",
    "el.hidden = !visible",
    "window.addEventListener('resize', onLayout, { passive: true })",
    "const price = Number.parseFloat(item.price) || 0",
    "if (Number(status) === 1) markPaid(order)",
    "listEl.replaceChildren(...nodes)",
    "const user = await api.get('/user/info')",
    "const query = { ...defaultQuery, ...filters }",
    "export function OrderList() { return useOrderState() }",
    "navigate(`/detail?id=${encodeURIComponent(row.id)}`)",
    "const date = new Date(row.createdAt).toISOString()",
    "if (!form.phone) throw new Error('phone required')",
    "try { await fetchList() } finally { setLoading(false) }",
    "headers.set('Authorization', `Bearer ${getToken()}`)",
    "const text = await fs.promises.readFile(configPath, 'utf8')",
    "app.use(express.json({ limit: '1mb' }))",
    "const user = await db.prepare('SELECT * FROM users WHERE id = ?').get(userId)",
    "const value = await redis.get(`session:${sid}`)",
    "export { getUser, updateUser, deleteUser }",
    "const config = await import(`../config/${env}.js`)",
    "if (err) return res.status(500).json({ error: err.message })",
    "const total = cart.items.reduce((sum, it) => sum + it.qty * it.price, 0)",
    "table.addEventListener('click', onRowClick)",
    "const clone = structuredClone(record)",
    "img.src = row.avatar ?? '/assets/default-avatar.png'",
    "url.searchParams.set('tab', tab)",
    "const qs = new URLSearchParams(params).toString()",
    "if (type === 'edit') await loadDetail(id)",
    "const search = debounce(load, 400)",
    "const pageCount = Math.max(1, Math.ceil(total / pageSize))",
    "el.classList.toggle('is-active', active)",
    "return fn(req, res, next)",
    "const idx = list.findIndex((item) => item.id === id)",
    "if (idx !== -1) list.splice(idx, 1)",
    "sessionStorage.setItem('filters', JSON.stringify(query))",
    "const file = req.files?.[0] ?? null",
    "res.type('application/json')",
    "const env = process.env.API_BASE ?? 'http://127.0.0.1:8080'",
    "watch(keyword, () => { page.value = 1; load() })",
    "onMounted(() => { load(); timer = setInterval(load, 15000) })",
    "onUnmounted(() => clearInterval(timer))"
  ],
  "bugs": [
    {
      "err": "TypeError: Cannot read properties of undefined",
      "fix": "fixed  foo?.bar ?? fallback  // 先确认它活着"
    },
    {
      "err": "ReferenceError: bug is not defined",
      "fix": "fixed  const bug = findBug()  // 先声明再骂它"
    },
    {
      "err": "FATAL: 屎山局部坍塌",
      "fix": "fixed  if (!pile) return  // 坍塌被挡在门口"
    },
    {
      "err": "Error: expected {, saw 💩",
      "fix": "fixed  换成 {  // 💩 请走注释通道"
    },
    {
      "err": "UnhandledPromiseRejection: 又双叒崩了",
      "fix": "fixed  .catch((e) => log(e))  // 崩完还能讲出来"
    },
    {
      "err": "SyntaxError: Unexpected token ; ; ;",
      "fix": "fixed  删掉多余分号  // 只留一个活口"
    },
    {
      "err": "RangeError: Maximum call stack size exceeded",
      "fix": "fixed  递归加了出口  // 不再自己 call 自己到天亮"
    },
    {
      "err": "Warning: 这段代码有自己的想法",
      "fix": "fixed  抽成纯函数  // 想法收编进参数"
    },
    {
      "err": "Error: ECONNRESET prod-db 被谁 rm 了",
      "fix": "fixed  只读副本顶上  // prod-db 先停刑"
    },
    {
      "err": "TypeError: foo is not a function, foo is foo",
      "fix": "fixed  typeof foo === 'function' && foo()  // foo 先证明自己"
    },
    {
      "err": "FATAL: git 拒绝覆盖同事的周末",
      "fix": "fixed  git pull --rebase  // 周末得以保全"
    },
    {
      "err": "Error: Cannot find module './utils/utils/utils'",
      "fix": "fixed  import from './utils'  // 少绕两层迷宫"
    },
    {
      "err": "Warning: setState on unmounted 情绪",
      "fix": "fixed  if (!alive) return  // 情绪不再 setState"
    },
    {
      "err": "SyntaxError: Unexpected end of JSON in 注释里",
      "fix": "fixed  JSON.parse 只吃字符串  // 注释请滚去 //"
    },
    {
      "err": "Error: 401 但本地 token 还活着",
      "fix": "fixed  刷新 token 再请求  // 本地活着不算数"
    },
    {
      "err": "RangeError: Invalid array length 999999999",
      "fix": "fixed  new Array(Math.min(n, 1024))  // 长度先限速"
    },
    {
      "err": "TypeError: Assignment to constant 临时变量",
      "fix": "fixed  let tmp = value  // 临时变量终于能变"
    },
    {
      "err": "Error: timeout of 0ms exceeded",
      "fix": "fixed  timeout: 8000  // 0ms 对谁都不礼貌"
    },
    {
      "err": "Warning: 循环依赖把自己 import 进来了",
      "fix": "fixed  拆出 types.js  // 自己不再 import 自己"
    },
    {
      "err": "FATAL: 构建过了，人没过",
      "fix": "fixed  补了测试才合  // 人和构建一起过"
    },
    {
      "err": "Error: ENOENT /the/file/we/swore/was-there.js",
      "fix": "fixed  existsSync 后再读  // 发誓改成检查"
    },
    {
      "err": "Unhandled: then() 里又 then() 里又 then()",
      "fix": "fixed  async/await 摊平  // then 金字塔拆了"
    },
    {
      "err": "Error: CORS 把锅甩给前端",
      "fix": "fixed  Access-Control-Allow-Origin  // 锅送回网关"
    },
    {
      "err": "Warning: memory leak 从 2019 活到现在",
      "fix": "fixed  removeEventListener  // 2019 的监听下班了"
    },
    {
      "err": "SyntaxError: missing ) after 我发誓有括号",
      "fix": "fixed  补上 )  // 发誓无效，括号有效"
    },
    {
      "err": "Error: 0 BUG 但 12 个 未知行为",
      "fix": "fixed  未知行为写成测试  // 0 BUG 不再装死"
    },
    {
      "err": "FATAL: 这行能跑，但谁也解释不了",
      "fix": "fixed  重写成能讲的版本  // 能跑也能解释"
    },
    {
      "err": "TypeError: null.forEach is not a vibe",
      "fix": "fixed  (list ?? []).forEach  // vibe 建立在数组上"
    }
  ]
}
