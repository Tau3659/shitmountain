# 屎山 · 精致像素风

构图和规则不动。只换材质：SNES/GBA 那档精致点阵，不是糊 PS1，也不是网页圆角。

## 参考图

参考图在分支 `art/pixel-direction`（PR #1）：`play.png` / `hud.png` / `scene.png`。图不进游戏包。

稿上的窗外城市、墙上长句海报、胶囊钮不要进游戏。

## 层

竖屏上半场景、下半当显示器。HUD 另叠，不烤进 160×120。

上半只画一张 `img/scene.png`（巫师程序员坐在桌前写代码，透明底叠在夜灰墙上）。改 bug 时整图叠一层 CRT 冷光。下半显示器：框钉死、磷光底、字可滚、扫描线。窗外城市和海报不进。

## 场景（钉死）

- 上半一张侧视点阵图：尖帽、青绿袍、背包、木凳木桌、手提电脑。人朝左。
- 场景按低分点阵放大（逻辑分辨率 160×120，nearest-neighbor）。
- HUD 头像从同一张图裁帽和脸，不再另备三档切片。

## HUD

- 顶栏左：职称 + 头像 + 存款。收工单里进道具 / 成就。一套外观、一个办公室。
- 全部硬边 9 宫窗：外 2px 亮/暗 bevel，不要圆角、不要 CSS 模糊。
- 点阵字 12px（现有 FusionPixel，运行时用裁切后的 woff2）。三数同字号同字重。
- 屎山 `#d0d4d8`；正确 `#48d060`；Bug>0 整块底 `#3a1c1c` 字 `#d45a4a`。
- 状态牌实底，贴头顶空白：写屎山 `#d4b45a` / 改 Bug `#48d060` / 写对的 `#48d060`。
- 收工：方钮，按下内凹 bevel，热区 ≥44。
- 终端：黑底磷光框。里面是源码：行号 + 语法高亮（关键字褐、字符串绿、数字黄、注释灰、函数名 CRT 青）。写屎山时正常代码中间稀疏夹 bug 行（红底，独白当行尾注释）。
- 通关章：方章，绿边，不要圆章。开除：方章，红边。
- 上半运行时只加载 `img/scene.png`。旧切片（头/身/手/键盘/CRT/三档脸/atlas）已删。

## 色板

| 用途 | 色 |
| 底 | `#12141a` |
| 墙 | `#1a1c1e` |
| 面板 | `#2a2d30` |
| 边 | `#7a7e82` |
| 字 | `#d0d4d8` |
| 正确 | `#48d060` |
| Bug | `#d45a4a` |
| 写屎山 | `#d4b45a` |
| CRT 光 | `#a8d4e8` |
| 关键字 | `#c58a6a` |
| 字符串 | `#48d060` |
| 数字 | `#d4b45a` |
| 注释 | `#8b9094` |
| 函数名 | `#a8d4e8` |
